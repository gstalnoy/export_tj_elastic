-  Пользователь ERP0\!СлужебныйОтражениеБУ. Можем ли вынести отражение бухгалтерских проводок в ночное время ?
-  Пользователь ERP0\!СлужебныйОтражениеБУ. Посмотреть, какие задания от него ещё запускаются для учёта.
- ERP0\1Служебный_Взаиморасчеты. Можно ли сократить частоту или вынести на ночь ?
- Вынос отдельных операций на DEV зависит от загрузки процессора ERP в период закрытия / фактирования.
Анализируем пики загрузки ERP процессора и принимаем решение.

- ОбщийМодуль.РеглУчетПроведениеСервер.Модуль : 1626 : ЗапросДанных.Выполнить() - выснить, кто запускает эту функцию ?
- Обработка.ДвиженияАктивовПассивов.МодульМенеджера : 187 : РезультатЗапроса = ЗапросДвижений.Выполнить()  - 
производство, услуги (вероятно, относится к процедуре закрытия периода) - вероятно, запускается пользователем ERP0\!СлужебныйОтражениеБУ


- разделить статистику по блокировкам в момент процедуры закрытия и без неё.
- проверить проведение пустых документов в ERP - С. Клочков.
- Задача по перенастройке APDEX:
1) удалить или отключить все базовые настройки APDEX отовсюду.
2) Сначала для типов "ДОКУМЕНТЫ" сделать новую настройку логирования по процедуре ОбработкаПроведения()
Взять первые 30 top документов по количеству записей в базе данных.
до реализации необходимо перейти на платформу 8.3.12
- проанализировать отдельное хранение файлов  в файловых группах
- записываем 2 часа ЦУП-ом данные по продолжительности или делаем кусками эти 2 часа
- проанализировать наполнение временных таблиц и имеющееся ограничение памяти сервера 128 Гб на их размещение
- имеется проблема некачественного написания кода по очистке темпоральных таблиц
- есть таблица "Прочии расходы незавершённого производства" огромного объёма, которая, вероятнее всего должна 
очищаться периодически при закрытии
- рекомендации ИТС посмотреть по настройкам сервера

Письмо С. Клочкова:

Блокировки. В файле обнаружил проблемы:
•	Частый обмен данными. Возможно импорт данных во время отражения в БУ или во время распределения взаиморасчетов.
•	Во время работы обработки ПТУпоРТУ идет отражение в БУ или распределение взаиморасчетов.
•	Во время отражения в БУ идет распределение взаиморасчетов.

Эти фоновые задания тесно связаны между собой, поэтому при блокировках решение: 
•	разнести во времени выполнение этих фоновых процессов
•	не запускать одно пока не закончилось другое .. 
•	может быть сделать какую-либо очередь выполнения
•	… 

Нагрузка.
По топ3 видно доминирование отражения в БУ и распределения взаиморасчетов. 
Возможно влияние взаимных блокировок, поэтому порядок такой:
1.	Провести работу по устранению  блокировок.
2.	Проверить отсутвие блокировок.
3.	Повторный анализ уже только в части нагрузки.
4.	Если необходимо, то оптимизация кода, делающего высокую нагрузку на сервер.
